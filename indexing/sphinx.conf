#
# Minimal Sphinx configuration sample (clean, simple, functional)
#

source mcdb_articles
{
	type			= xmlpipe2

	# shell command to invoke xmlpipe stream producer
	# mandatory
	#
	xmlpipe_command		= ./mongo2sphinx.py --db MCDB --col articles --fields summary title link_content link _id --attrs links language published _id
	xmlpipe_fixup_utf8	= 1
}
source mcdb_feeds
{
	type			= xmlpipe2

	# shell command to invoke xmlpipe stream producer
	# mandatory
	#
	xmlpipe_command		= ./mongo2sphinx.py --db MCDB --col feeds --fields title link --attrs links language published _id

	xmlpipe_fixup_utf8	= 1
}


index MCDB_ARTICLES
{
    type            = plain
	source			= mcdb_articles
	path			= ./index_articles/
	html_strip = 1
	html_remove_elements = style, script
	html_index_attrs = img=alt,title; a=title
}

index MCDB_FEEDS
{
    type = plain
	source			= mcdb_feeds
	path			= ./index_feeds/
	docinfo			= extern

}



indexer
{
	mem_limit		= 32M
}


searchd
{
   # listen          =127.0.0.1
    #listen          =127.0.0.1:9312
	listen			= 9312
	listen			= 9306:mysql41
	log			= ./log/searchd.log
	query_log		= ./log/query.log
	read_timeout		= 5
	max_children		= 30
	pid_file		= ./log/searchd.pid
	max_matches		= 1000
	seamless_rotate		= 1
	preopen_indexes		= 1
	unlink_old		= 1
	workers			= threads # for RT to work
}
